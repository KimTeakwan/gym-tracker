spring:
  datasource:
    # 맥북 로컬 MySQL 주소임!
    url: jdbc:mysql://localhost:3306/gym_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Kk5534264@

  jpa:
    hibernate:
      # update: 자바 코드를 보고 DB 테이블을 알아서 고쳐주거나 만들어줌!
      ddl-auto: update
    show-sql: true # 터미널에 SQL 문이 찍히게 해서 공부하기 좋게 했음~
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false # 코드 고치고 새로고침하면 바로 반영되게 꺼놨음!